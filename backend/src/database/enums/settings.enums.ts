export enum SettingScope {
    SYSTEM = 'system',
    USER = 'user',
    ORGANIZATION = 'organization'
}

export enum SettingType {
    STRING = 'string',
    NUMBER = 'number',
    BOOLEAN = 'boolean',
    JSON = 'json',
    ARRAY = 'array'
}