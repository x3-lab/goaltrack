export enum UserRole {
    ADMIN = 'admin',
    VOLUNTEER = 'volunteer',
}

export enum UserStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum Performance {
    HIGH = 'high',
    AVERAGE = 'average',
    LOW = 'low',
}